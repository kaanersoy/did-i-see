{
  "name": "did-i-see",
  "version": "1.0.0",
  "description": "A lightweight function that executes callback when we see specific DOM elements.",
  "main": "src/index.js",
  "repository": "git@github.com:kaanersoy/did-i-see.git",
  "author": "kaanersoy <kaanersoy12@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d dist",
    "build:watch": "babel src --watch -d dist",
    "copy-demo-files": "cp -R src/demo/index.html dist/demo/",
    "build:production": "yarn build && yarn copy-demo-files"
  },
  "dependencies": {
    "@babel/cli": "^7.14.5",
    "@babel/core": "^7.14.6",
    "babel-preset-minify": "^0.5.1"
  }
}
